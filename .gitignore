# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv*/
venv/
env/

# Local environment
.env
.env.*

# Generated benchmark data
data/*.jsonl
data/queries_review.txt
data/*thinking_example*.json
data/*thinking_example*.txt
data/*thinking*/
data/audit/*.jsonl
data/audit/*.json
data/audit/*.md
!data/audit/README.md
data/audit/query_ids.txt

# Phase 4 per-retriever evaluation artifacts (regenerable)
data/per_retriever/
data/per_retriever_labels/

# Tier 2 full-corpus retriever rankings (regenerable from scripts/)
data/full/
data/all_query_ids.txt
# Kenya real-clinician query set (generated; .jsonl siblings already ignored)
data/kenya_ids.txt
# Raw-response side files from v2_graded judge runs (potentially large)
*.raw.jsonl

# Retrieval/index artifacts
*.faiss
*.index
*.npy
*.npz
.cache/
cache/
data/candidates*.jsonl

# HF release bundles — large parquet artifacts and preview folders are
# regenerable via scripts/build_hf_release.py. Only hand-authored content
# (README, LICENSE, manifest, verbatim prompt files) is tracked.
releases/*-previews/
releases/*/data/
releases/*/audit/*.parquet

# Logs and local outputs
*.log
tmp/
dist/
build/

# macOS
.DS_Store
